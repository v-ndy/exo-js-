'use strict';

/**
 * Exercice 1
 */

/**
 * Returns the sum of string 
 * @param {arg} string
 * @param {arg} number
 * @param {alert} si number different string so poster message 
 * @returns {string} no printed alert.
 */

var myPutStr = function myPutStr(string, number) {

    if (number > string) {
        alert("et pourquoi pas 42 ?")
}

    return string;
};

var myCalculator = function computeSurfaceM2 (L,l){
   
    return  L * l * 3;
};
